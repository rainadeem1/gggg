
@using (Html.BeginForm("SendMsg", "Mobile",FormMethod.Post, new { id = "frm-send" }))
{

    <table class="table">
        <tr>
            <td>
                @Html.RadioButton("To", "std", new {  })
            <label for="radio1">To All Students</label>
        </td>
        <td>
            @Html.RadioButton("To", "tchr", new {  })
        <label for="radio2">To All Teachers</label>
    </td>
</tr>
</table>
    @*<div class="form-group">
        <label>To</label>
        <div class="input-group">
            @Html.DropDownList("classes",
ViewBag.AllClasses as List<SelectListItem>, new { @class = "form-control chosen", style = "width:250px",multiple = "multiple" })

        </div>
    </div>*@

    <div class="form-group">
        <label>Mobile Number</label>
        <div class="input-group">
            @Html.TextBox("Number", null, new { @class = "form-control" })

        </div>
    </div>
                    <div class="form-group">
                        <label>Message</label>
                        <div class="input-group">
                            @Html.TextArea("msg", null, new { @class = "form-control", cols = "40" })

                        </div>
                    </div>
                    <table>
                        <tr>
                            <td><button type="reset" class="btn btn-block btn-default">CLEAR</button></td>
                            <td><button type="submit" class="btn btn-block btn-success">SEND</button></td>
                        </tr>
                    </table>
}




@if (SNS.DAL.Shared.Message != null)
{
    <div class="alert alert-info alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>

        @SNS.DAL.Shared.Message
    </div>
    SNS.DAL.Shared.Message = null;
}